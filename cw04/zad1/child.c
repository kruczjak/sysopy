#include<stdio.h>
#include<stdlib.h>
int usr2sig = 0;

int main(int argc, char ** argv) {

  while (usr2sig) {
    
  }

  for(int i=0; i<n; i++) {
     kill(child, SIGUSR1);
  }
  kill(child, SIGUSR2);
  wait(NULL);

  return 0;
}
